@using Epam.Task11.WebPages.CS;

@{
    if (IsAjax)
    {
        int.TryParse(Request.Params["id"], out var id);

        if (id > 0)
        {
            if (BLLProvider.UserLogic.Remove(id))
            {
                @(1)
            }

            return;
        }
    }
}