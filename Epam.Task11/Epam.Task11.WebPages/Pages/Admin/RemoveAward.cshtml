@if (!User.IsInRole("admin"))
{
    Response.Redirect("~");
}

<div class="form">
    <form method="post" id="form" onsubmit="acceptChoice('Are you sure?', getId, this, '/Pages/Ajax/RemoveAwardAjax')">
        
        @RenderPage("/Pages/Partial/_IdFieldPartial.cshtml", new { Message = "Enter award ID: ", Id = "id" })

        <button type="submit" id="submit">Remove award</button>
    </form>
</div>

<div id="result"></div>