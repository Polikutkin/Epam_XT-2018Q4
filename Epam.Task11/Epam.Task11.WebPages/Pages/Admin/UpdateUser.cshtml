@if (!User.IsInRole("admin"))
{
    Response.Redirect("~");
}

<div class="form">
    <form method="post" id="form" onsubmit="sendUserInfo(this, '/Pages/Ajax/UpdateUserAjax')">
        
        @RenderPage("/Pages/Partial/_IdFieldPartial.cshtml", new { Message = "Enter user ID: ", Id = "id" })
        @RenderPage("/Pages/Partial/_NewUserFieldsPartial.cshtml")

        <button type="submit" id="submit">Update user</button>
    </form>
</div>

<div id="result"></div>

@RenderPage("/Pages/Partial/_UserNotesPartial.cshtml")