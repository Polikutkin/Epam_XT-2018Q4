@if (!User.IsInRole("admin"))
{
    Response.Redirect("~");
}

<div class="form">
    <form method="post" id="form" onsubmit="getId(this, '/Pages/Ajax/RemoveUserAjax')">
        
        @RenderPage("/Pages/Partial/_IdFieldPartial.cshtml", new { Message = "Enter user ID: ", Id = "id" })

        <button type="submit" id="submit">Remove user</button>
    </form>
</div>

<div id="result"></div>